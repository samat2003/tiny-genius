# Stage 0 / development configuration.
# Not the frozen 300M training configuration (that is Stage 9).

name: stage0-dev
stage: 0
frozen: false

project: Tiny Genius

reproducibility:
  seed: 42
  deterministic: true

logging:
  level: INFO

# Paths are relative to the repository root. Large outputs stay untracked.
paths:
  artifacts: artifacts
  runs: runs
