# Artifacts

Generated run artifacts, checkpoints, and experiment outputs belong here locally
and **must not** be committed.

The Git repository stores code, configs, manifests, hashes, and reports.
Raw datasets and large checkpoints stay outside version control.

This directory exists so later stages have a conventional output location.
